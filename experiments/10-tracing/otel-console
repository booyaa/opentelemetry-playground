#!/bin/bash

docker compose --file docker-compose-otel-console.yml up --detach

clear

echo "💡 Click on http://0.0.0.0:4567/ to generate some transactions"
echo "👀 Click on http://127.0.0.1:9411/zipkin to see how traces are visualised using zipkin"
echo "👀 Click on http://127.0.0.1:16686/ to see how traces are visualised using jaeger"
echo "Which do you prefer?"

# shellcheck disable=SC2162
read -p "Press enter to stop experiment"

docker compose down
