receivers:
  filelog:
    include: [ "/root/*.log" ]
exporters:
  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: none
  pipelines:
    logs:
      receivers: [filelog]
      exporters: [debug]
